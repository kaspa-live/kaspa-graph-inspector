version: '3'
services:
  postgres:
    image: "postgres:13.2"
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    user: root
    network_mode: host

  processing:
    image: "kaspa-dag-visualizer-processing:latest"
    depends_on:
      - postgres
    command:
      - "/app/processing"
      - "--devnet"
      - "--p2p-server=${KASPAD_ADDRESS}"
      - "--connection-string=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?sslmode=disable"
    network_mode: host
    user: root
    restart: always

  api:
    image: "kaspa-dag-visualizer-api:latest"
    depends_on:
      - postgres
      - processing
    environment:
      - PGUSER=${POSTGRES_USER}
      - PGPASSWORD=${POSTGRES_PASSWORD}
      - PGDATABASE=${POSTGRES_DB}
    command:
      - "npm"
      - "run"
      - "--prefix"
      - "/app"
      - "start"
    network_mode: host
    user: root
    restart: always

  web:
    image: "kaspa-dag-visualizer-web:latest"
    depends_on:
      - postgres
      - processing
      - api
    command:
      - "npm"
      - "run"
      - "--prefix"
      - "/app"
      - "start"
    network_mode: host
    user: root
    restart: always
